export const Term = () => {
    return (
        <div>
            <label>
                Count of months:
                <input id="monthCount" type="number" required placeholder="Enter the number of months" />
            </label>
        </div>
    )
}